export default function DesignChangeDashboard() {
  const stats = [
    { title: 'Total Changes', value: '124', sub: '+12 this week' },
    { title: 'Pending Approvals', value: '18', sub: '5 urgent' },
    { title: 'Overdue Items', value: '6', sub: 'Needs attention' },
    { title: 'Approved This Month', value: '45', sub: '+18%' },
  ];

  const changes = [
    {
      no: 'DC-024',
      project: 'Condo A',
      title: 'Lobby Ceiling Revision',
      status: 'Pending Client',
      pending: 'Client Team',
      priority: 'High',
      due: '29 May 2026',
    },
    {
      no: 'DC-025',
      project: 'Office Tower B',
      title: 'HVAC Routing Update',
      status: 'Revision Required',
      pending: 'MEP Engineer',
      priority: 'Medium',
      due: 'Overdue 3 days',
    },
    {
      no: 'DC-026',
      project: 'Hotel C',
      title: 'Material Specification Change',
      status: 'Approved',
      pending: '-',
      priority: 'Low',
      due: 'Completed',
    },
  ];

  const activities = [
    'Architect uploaded Rev.B drawing',
    'Project Manager approved DC-024',
    'Client requested revision for HVAC routing',
    'New change request created by Interior Team',
  ];

  const getStatusStyle = (status) => {
    if (status.includes('Pending')) return 'bg-yellow-100 text-yellow-800';
    if (status.includes('Revision')) return 'bg-red-100 text-red-800';
    if (status.includes('Approved')) return 'bg-green-100 text-green-800';
    return 'bg-gray-100 text-gray-700';
  };

  return (
    <div className="min-h-screen bg-gray-100 p-6">
      {/* Header */}
      <div className="flex items-center justify-between mb-8">
        <div>
          <h1 className="text-4xl font-bold text-gray-900">
            Design Change Dashboard
          </h1>
          <p className="text-gray-600 mt-1">
            Track revisions, approvals, and pending workflows
          </p>
        </div>

        <div className="flex gap-3">
          <button className="px-4 py-2 bg-white rounded-2xl shadow-sm border">
            Export Report
          </button>
          <button className="px-4 py-2 bg-black text-white rounded-2xl shadow-sm">
            + New Change
          </button>
        </div>
      </div>

      {/* KPI Cards */}
      <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-5 mb-8">
        {stats.map((item) => (
          <div
            key={item.title}
            className="bg-white rounded-3xl p-6 shadow-sm border border-gray-200"
          >
            <p className="text-gray-500 text-sm">{item.title}</p>
            <h2 className="text-4xl font-bold mt-2">{item.value}</h2>
            <p className="text-sm text-gray-400 mt-3">{item.sub}</p>
          </div>
        ))}
      </div>

      {/* Main Grid */}
      <div className="grid grid-cols-1 xl:grid-cols-4 gap-6">
        {/* Main Table */}
        <div className="xl:col-span-3 bg-white rounded-3xl shadow-sm border border-gray-200 overflow-hidden">
          <div className="p-6 border-b border-gray-100 flex items-center justify-between">
            <div>
              <h2 className="text-2xl font-semibold">Active Change Requests</h2>
              <p className="text-gray-500 text-sm mt-1">
                Current revisions and approval status
              </p>
            </div>

            <input
              type="text"
              placeholder="Search changes..."
              className="border rounded-xl px-4 py-2 w-64"
            />
          </div>

          <div className="overflow-x-auto">
            <table className="w-full text-left">
              <thead className="bg-gray-50 text-gray-600 text-sm">
                <tr>
                  <th className="px-6 py-4">Change No</th>
                  <th className="px-6 py-4">Project</th>
                  <th className="px-6 py-4">Title</th>
                  <th className="px-6 py-4">Status</th>
                  <th className="px-6 py-4">Pending From</th>
                  <th className="px-6 py-4">Priority</th>
                  <th className="px-6 py-4">Due Date</th>
                </tr>
              </thead>

              <tbody>
                {changes.map((change) => (
                  <tr
                    key={change.no}
                    className="border-t border-gray-100 hover:bg-gray-50 transition"
                  >
                    <td className="px-6 py-5 font-semibold">{change.no}</td>
                    <td className="px-6 py-5">{change.project}</td>
                    <td className="px-6 py-5">{change.title}</td>
                    <td className="px-6 py-5">
                      <span
                        className={`px-3 py-1 rounded-full text-sm font-medium ${getStatusStyle(
                          change.status
                        )}`}
                      >
                        {change.status}
                      </span>
                    </td>
                    <td className="px-6 py-5">{change.pending}</td>
                    <td className="px-6 py-5">{change.priority}</td>
                    <td className="px-6 py-5">{change.due}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>

        {/* Sidebar */}
        <div className="space-y-6">
          {/* Notifications */}
          <div className="bg-white rounded-3xl p-6 shadow-sm border border-gray-200">
            <h2 className="text-xl font-semibold mb-4">Notifications</h2>

            <div className="space-y-3">
              <div className="p-4 rounded-2xl bg-yellow-50 border border-yellow-100">
                <p className="font-medium">3 items near deadline</p>
                <p className="text-sm text-gray-500 mt-1">
                  Review required today
                </p>
              </div>

              <div className="p-4 rounded-2xl bg-red-50 border border-red-100">
                <p className="font-medium">2 overdue revisions</p>
                <p className="text-sm text-gray-500 mt-1">
                  Waiting for consultant update
                </p>
              </div>
            </div>
          </div>

          {/* Activity Feed */}
          <div className="bg-white rounded-3xl p-6 shadow-sm border border-gray-200">
            <h2 className="text-xl font-semibold mb-4">Activity Feed</h2>

            <div className="space-y-4">
              {activities.map((activity, index) => (
                <div key={index} className="flex gap-3 items-start">
                  <div className="w-3 h-3 rounded-full bg-black mt-2" />
                  <div>
                    <p className="text-sm font-medium">{activity}</p>
                    <p className="text-xs text-gray-400 mt-1">10 mins ago</p>
                  </div>
                </div>
              ))}
            </div>
          </div>

          {/* Workflow Summary */}
          <div className="bg-white rounded-3xl p-6 shadow-sm border border-gray-200">
            <h2 className="text-xl font-semibold mb-4">Workflow</h2>

            <div className="space-y-4 text-sm">
              <div className="flex items-center justify-between">
                <span>Draft</span>
                <span className="font-semibold">12</span>
              </div>

              <div className="flex items-center justify-between">
                <span>Pending Review</span>
                <span className="font-semibold">18</span>
              </div>

              <div className="flex items-center justify-between">
                <span>Revision Required</span>
                <span className="font-semibold">7</span>
              </div>

              <div className="flex items-center justify-between">
                <span>Approved</span>
                <span className="font-semibold">45</span>
              </div>

              <div className="flex items-center justify-between">
                <span>Closed</span>
                <span className="font-semibold">42</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
